function Nvi()  { 
    if pushd ${modHOME}.custom/.NoteDir/. ; then
        if [ $# -lt 1 ] ; then
            vi . ;
        else
            vi $( ls | fgrep $1 ) ;
        fi ;
        popd ;
    fi ;
}

