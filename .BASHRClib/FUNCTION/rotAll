function rotALL ()  {
    jnk="$@"
[ "$XXX_MIKE_DEBUG" ] && echo ARG0 is "${jnk}"
    x=26
    while [ $x -gt 0 ]
    do
        echo "${jnk}"
        jnk="$(echo "${jnk}" | rot1)"
        let x--
    done
}
